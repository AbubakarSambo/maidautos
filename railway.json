{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -d api ]; then cd api; fi && npm install"
  },
  "deploy": {
    "startCommand": "if [ -d api ]; then cd api; fi && npx prisma migrate deploy && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
